fn main() {
    let sum = 5 + 10;

    let diff = 95.5 - 4.3;

    let product = 4 * 30;

    let rem = 43 % 5;

    let t = true;

    dbg!(sum, diff, product, rem, t);

    let tup: (i32, f64, u8) = (500, 6.4, 1);
    dbg!(tup);
}
